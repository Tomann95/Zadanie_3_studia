using System;

class Program
{
    static void Main()
    {
        
        int[] tablica =  new int[10];
        Random rand = new Random();
        for (int i = 0; i < tablica.Length; i++)
        {
            tablica[i] = rand.Next(1, 100); i: 1-100
        }

   
        for (int i = tablica.Length - 1; i >= 0; i--)
        {
            Console.WriteLine(tablica[i]);
        }

        Console.ReadLine();
    }
}


